#define _USE_MATH_DEFINES
#include <iostream>
#include <cmath>
#include <locale>

using namespace std;

int main() {
	setlocale(LC_ALL, "uk-UA");
	double radius, height;
	double volume, area;

	cout << "Ââĺäiňü đŕäióń îńíîâč öčëiíäđŕ: ";
	cin >> radius;

	cout << "Ââĺäiňü âčńîňó öčëiíäđŕ: ";
	cin >> height;

	volume = M_PI * radius * radius * height;
	area = 2 * M_PI * radius * height;

	cout << "Îá'şě öčëiíäđŕ: " << volume << endl;
	cout << "Ďëîůŕ áîęîâîż ďîâĺđőíi öčëiíäđŕ: " << area << endl;

	return 0;
